[package]
name = "customwiz"
version = "0.1.0"
edition = "2024"

[dependencies]
quiz-engine = { path = "../quiz-engine" }
core = { path = "../core" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
chrono = "0.4"

[dev-dependencies]
tempfile = "3.16.0"
